{
  "name": "@project-starter/docs",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vitepress dev --host",
    "build": "vitepress build",
    "generate": "vitepress build",
    "preview": "vitepress preview --host",
    "clear": "rimraf .vitepress/dist; rimraf .vitepress/cache || true"
  },
  "dependencies": {
    "vitepress": "catalog:"
  }
}
